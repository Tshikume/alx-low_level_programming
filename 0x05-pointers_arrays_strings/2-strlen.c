#include "main.h"

/**
 *int _strlen - returns the length of a string
 *@s - string to return lenghth of
 *Return: length
 */
int _strlen(char *s)
{
int length = 0;
while (str[length] != '\0') 
{
length++;
}
return (length);
}

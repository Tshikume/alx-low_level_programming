#include <stdio.h>
#include "main.h"
/**
* _abs - a function that computes the absolute value of an integer.
 * @c: The character is an absolute value
 *
 * Return: absolute value of an integer.
*/
int _abs(int c)
{
if (c >= 0)
return (c);
else
return (-c);
}
